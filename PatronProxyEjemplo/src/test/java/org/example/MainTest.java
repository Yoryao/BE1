package org.example;

import org.junit.Test;

class MainTest {

    @Test
    void seConecta(){


/*

       List<String> sitiosBloqueados = new ArrayList<>();
        sitiosBloqueados.add("www.youtube.com");
        sitiosBloqueados.add("www.google.com");

        IConexionInternet proxy;
        proxy = new ProxyInternet(sitiosBloqueados, new InternetService());


        proxy.ConectarCon("www.google.com");
        proxy.ConectarCon("www.facebook.com");











    void esMayor(){
        Persona p = new Persona("Jote","hota", "mail", 20 );
        Persona q = new Persona("Jo","ho", "mail", 13 );
        Persona r = new Persona("te","ha", "mail", 15 );

        boolean mayor = p.isOlder();
        boolean mayorq = q.isOlder();
        boolean mayorr = r.isOlder();

        assertEquals(true, mayor);
        assertEquals(false, mayorq);
        assertFalse(mayorr);

*/

    }
}